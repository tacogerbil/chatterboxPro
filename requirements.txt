# =================================================================
# Requirements for Chatterbox Pro
# To install: pip install -r requirements.txt
# =================================================================

# --- Core PyTorch Libraries ---
# The extra index URL helps pip find the correct PyTorch version for your system
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchaudio>=2.0.0

# --- Core ML & TTS Model Libraries ---
transformers>=4.46.3
peft>=0.10.0
diffusers==0.29.0
faster-whisper>=1.0.0
s3tokenizer==0.1.7
omegaconf==2.3.0
einops==0.8.1
conformer==0.3.2
bitsandbytes>=0.43.0

# --- Audio & Signal Processing ---
librosa==0.10.0
pydub==0.25.1
scipy==1.15.3
scipy==1.15.3
resemble-perth==1.0.1
soundfile==0.12.1
pedalboard>=0.9.14 # For "Batman Gravel" / Narrator Polish chain

# --- GUI Libraries ---
# customtkinter lines removed
pygame==2.6.1

# --- Qt Migration ---
PySide6==6.8.1
pyqtgraph==0.13.7
qt-material

# --- Text & File Processing ---
sentence-splitter==1.4
pdftextract==0.0.5
EbookLib==0.18
beautifulsoup4==4.12.3

# For DOCX/MOBI support (also requires system installation of Pandoc)
pypandoc==1.13

# --- Post-Processing Tools ---
# Requires system installation of FFmpeg
ffmpeg-python==0.2.0

# For silence removal (optional but recommended)
auto-editor==27.1.1

# --- TTS Engines ---
# MOSS-TTS (loaded via Transformers)
# Requires flash-attn for optimized performance (optional but recommended)
flash-attn>=2.5.0

# =================================================================
# System Dependencies (install separately):
# - FFmpeg: Required for audio normalization and assembly
# - Pandoc: Required for .docx and .mobi file support
# =================================================================